import { SimpleHotelWorkspace } from '@/components/ciq/travel/SimpleHotelWorkspace'

export const metadata = {
  title: 'Hotels — cash or points | CreditIQ',
  description: 'Choose the property first, then compare a real cash rate with a verified loyalty redemption path.',
}

export default function HotelsPage() {
  return <SimpleHotelWorkspace />
}
