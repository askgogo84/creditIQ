# CreditIQ Read-Only Audit — 24 Aug 2026

Scope: read-only inventory audit across four sections. **No code, schema, or data was modified.** Only this file was created.

Method: Section 2 and the transfer-edge half of Section 4 were read in full by hand (the NEW_CARDS block was read line-by-line, not grep-filtered). Sections 1, 3, and the seats.aero-slug half of Section 4 were produced by parallel search agents and are attributed as such. Where an agent's count disagreed with a hand read, the hand read wins and the discrepancy is flagged.

---

## SECTION 1 — Every hardcoded data registry in the repo

> Source: search agent, cross-checked against hand reads of `lib/data/seed-cards.ts` and `lib/data/transfer-graph.ts`. **Correction applied:** the agent reported NEW_CARDS as "38 distinct ids." That is wrong — it counted only the indented verbose batch and missed the 44 compact one-line entries (the exact failure the audit brief warned about). The authoritative figure from the full hand read is **82 physical entries → 76 distinct ids** (see Section 2).

### Scale summary
- **Card registries:** 15+ across 3 locales (India live 49, UAE 16, dead NEW_CARDS 76-distinct) — **6 non-reconciling card id-spaces**
- **City→IATA maps:** 6 (4 primary + 1 nested dupe + 1 typo-alias) — none use the canonical resolver
- **Airport arrays:** 5 hardcoded (+1 canonical 3,242-row dataset only 2 components consume)
- **Airline/programme maps:** 5, in 3 different slug vocabularies
- **Per-point value stores:** 5 independent (disagree on KrisFlyer/Marriott/Axis)
- **Transfer-ratio stores:** 4 (disagree on Axis→KrisFlyer: 5:2 vs 5:4 vs 1.0)
- **Bank/issuer lists:** 5 (membership 6 vs 12 vs 13) + 2 bank-metadata registries
- **Affiliate/booking-URL maps:** 4
- **Build-gated (validated) registries:** only 2 — `TRANSFER_EDGES` and `CARD_POINT_VALUES`

### 1.1 Card registries

| # | Registry | File:line | Count | Consumers | Status |
|---|----------|-----------|-------|-----------|--------|
| C1 | `SEED_CARDS` | `lib/data/seed-cards.ts:6` | 19 base → **49 runtime** | ~50 files (whole app); `id===slug` | LIVE canonical |
| C2 | `MORE_CARDS` | `lib/data/seed-cards.ts:712` | 30 | only `:772` `SEED_CARDS.push(...MORE_CARDS)` | LIVE (merged into C1) |
| C3 | `APR_MAP` | `lib/data/seed-cards.ts:776` | 51 keys | `:831` forEach patches apr/employment/interest-free | LIVE |
| C4 | `NEW_CARDS` | `lib/data/seed-cards.ts:853` | **82 entries / 76 distinct ids** | **NONE** — declared, never spread | **DEAD** |
| C5 | `UAE_CARDS` (+`UAE_BANKS`) | `lib/data/uae-cards.ts:9,35` | 16 cards / 7 banks | only `app/uae/page.tsx` | LIVE (separate locale) |
| C6 | `CARDS_WITH_POINTS` | `app/(shell)/(spend)/points-optimizer/page.tsx:8` | 20 | that page's picker + AI prompt | LIVE (drifted ids) |
| C7 | `CARD_POINT_VALUES` | `lib/data/point-values.ts:158` | 7–10 rows (HDFC only) | `lib/rag.ts`; gated by `scripts/validate-point-values.ts` | LIVE (own id-space, matched by name) |
| C8 | `CURATED` | `app/(shell)/(cards)/card-roast/page.tsx:48` | 6 | that page | LIVE |
| C9 | `EDITORIAL_IDS` | `components/ciq/EditorialCards.tsx:19` | 6 | that component | LIVE |
| C10 | `LADDER` | `app/card-ladder/page.tsx:12` | 15 | that page | LIVE (stale hand-typed copy) |
| C11 | scraper `*_CARDS` | `lib/scrapers/hdfc.ts:7` (+axis/icici/sbi) | ~19 slugs | `runScrapers` → `app/api/scrape` | LIVE (bank-URL slugs) |
| C12 | inline name-list in AI prompt | `app/api/cards-sync/route.ts` (~:40) | ~90+ names | discovery prompt (free text) | LIVE |
| C13 | `UNVERIFIED_CARD_FIELDS` | `lib/data/unverified-cards.ts:14` | 4 | field-uncertainty overlay | LIVE |
| C14 | `MY_CARDS` (demo) | `app/(shell)/(travelgrp)/lounge-tracker/page.tsx:7` | 3 | that page | LIVE demo |
| C15 | `CARD_ART_SLUGS` | `lib/data/card-art-manifest.ts:9` | 0 (empty Set) | `hasCardArt` | LIVE but empty |

Cards actually served: **49 India (C1+C2) + 16 UAE (C5).** Everything else is dead (C4), non-catalog (C11/C12), or demo.

### 1.2 Airport / city-IATA / airline registries

Canonical dataset:
| # | Registry | File:line | Count | Consumers |
|---|----------|-----------|-------|-----------|
| A1 | `AIRPORTS_RAW` (OurAirports) | `lib/data/airports.generated.ts:22` | **3,242** | `lib/data/airports.ts:9` re-exports as `AIRPORTS`; only **2** UI consumers: `AirportSelect.tsx`, `Board.tsx` |
| A2 | `airport-aliases.json` | `lib/data/airport-aliases.json` | 31 | merged into A1 at build by `scripts/gen-airports.ts` |
| A3 | `POPULAR` (hub ranking) | `lib/data/airports.ts:51` | 42 | internal to `searchAirports()` |

City→IATA maps (6 — none use A1's `resolveCity`):
| # | Registry | File:line | Count |
|---|----------|-----------|-------|
| A4 | `CITY_TO_IATA` | `app/api/travel-ai/route.ts:16` | 34 keys (only map with EU secondary hubs) |
| A5 | `CITY_IATA` | `app/api/user-city/route.ts:12` | ~38–51 keys (India tier-2 deep; zero international) |
| A6 | `CITY_IATA` (module) | `app/api/trip-compare/route.ts:41` | 33 keys ("superset" claim is false) |
| A6b | `CITY_IATA` (nested in `buildKayakUrl`) | `app/api/trip-compare/route.ts:94` | 20 keys — strict subset dupe of A6 |
| A7 | `DEST_CODES` | `app/api/trip-planner/route.ts:44` | 19 keys |
| A7b | `aliases` (typo→city) | `app/api/trip-planner/route.ts:11` | 8 keys |

Plus 3 more city maps embedded in trip-compare URL builders: `CITY_MMT` (`:118`), `CITY` (`:135`), `CITY` (`:149`).

Hardcoded airport arrays (5):
| # | Registry | File:line | Count | Status |
|---|----------|-----------|-------|--------|
| A8 | `AIRPORTS` `{code,city}` | `app/onboarding/page.tsx:12` | 8 | LIVE |
| A9 | `AIRPORTS` `{code,name}` | `components/design/FlightSearch.tsx:115` | 44 | **DEAD** (FlightSearch imported nowhere); has label bugs (GAU→"LGB" etc.) |
| A10 | `ORIGINS` | `components/ciq/LiveAwardRow.tsx:32` | 8 | LIVE |
| A11 | `BOOKING_OPTIONS` | `components/design/FlightSearch.tsx:28` | 4 | DEAD (with A9) |

Airline / programme maps (5, three slug vocabularies):
| # | Registry | File:line | Count | Kind |
|---|----------|-----------|-------|------|
| A12 | `AIRLINE_TO_PROGRAM` | `app/api/travel-ai/route.ts:29` | 18 | name→program-slug |
| A13 | `AIRLINE_NAMES` | `lib/fares.ts:73` | ~38 | IATA→name |
| A14 | `AIRLINE_NAMES` (dupe) | `app/api/trip-compare/route.ts:60` | ~40 | IATA→name |
| A15 | `PROGRAM_TO_SOURCE` | `lib/seats-aero.ts:8` | 10 | program→seats.aero-slug (self-inverted) |
| A16 | `SOURCE_PROGRAM_LABEL` | `lib/fusion-core.ts:22` | 26 | slug→display-name |

Price tables (domain data): `FLIGHT_PRICE_RANGES` (`trip-compare:9`, 14+default), `HOTEL_PRICE_RANGES` (`trip-compare:27`, 8+default).

### 1.3 Rewards / transfer / affiliate registries

| # | Registry | File:line | Count | Consumers |
|---|----------|-----------|-------|-----------|
| R1 | `AFFILIATE_LINKS` | `lib/affiliate.ts:6` | ~135 slug→URL | `resolveAffiliate`/`getApplyUrl` → CardTile, apply API, compare; audited by 2 scripts |
| R2 | `TRANSFER_EDGES` | `lib/data/transfer-graph.ts:41` | **5 edges** | fusion-core, transfer-ladder, rag; **build-gated** |
| R3 | `TRANSFER_MAP` | `lib/transfer-map.ts:61` | 3 sources / 5 routes | `app/api/transfer-map`, `cardPointsFor` |
| R4 | `getDefaultRedemptions` tables | `lib/redemption.ts:16` | ~16 branches / ~55 rows | `getRedemptionOptions` → app/page, ProductTabs |
| R5 | `getSmartRedemptions.allOptions` | `lib/redemptions.ts:32` | 12 | **PARKED/dead** (no live consumer) |
| R6 | `PROGRAMS` (loyalty) | `app/loyalty-tracker/page.tsx:13` | 12 | that page (own valuePerPoint/sweetSpot) |
| R7 | `TRANSFER_PARTNERS` | `app/api/trip-compare/route.ts:183` | 6 banks | that route (all ratios flat 1.0) |
| R8 | `BOOKING_LANDING` | `components/ciq/fly-points/Board.tsx:103` | 13 slug→URL | `bookingUrl()` |
| R9 | `PROGRAM_SITE` | `app/flights/page.tsx:94` | 3 slug→URL | flights page |
| R10 | `CURRENCY_LABEL` | `app/(shell)/(travelgrp)/transfer-partners/page.tsx:28` | 5 | that page |
| R11 | `BANK_ALIASES` | `lib/transfer-map.ts:161` | 5 | transfer resolution |
| — | per-card `redemption_options` in SEED_CARDS | `lib/data/seed-cards.ts` | 158 `value_per_point_inr` values, 26 ratio strings | 5th per-point-value store |

Only R2 and C7 are build-gated. Everything else is unvalidated.

### 1.4 Bank / issuer registries (heavy duplication)

| # | Registry | File:line | Count |
|---|----------|-----------|-------|
| B1 | `BANKS` `{slug,name,color}` | `app/(shell)/banks/page.tsx:6` | 12 |
| B2 | `BANKS` `{id,name,color,pwdHint}` | `app/upload-statement/page.tsx:48` | 12 |
| B3 | `BANKS` + `BANK_COLORS` | `app/(shell)/(wallet)/dashboard/page.tsx:17,12` | 13 (adds "Other") |
| B4 | `BANKS` (string[]) | `app/sitemap.ts:13` | 12 |
| B5 | `BANK_COLORS` | `app/sms-import/page.tsx:87` | 6 |
| B6 | `BANK_SENDERS` | `app/sms-import/page.tsx:19` | 6 |
| B7 | `BANK_META` `{name,color,desc}` | `app/(shell)/banks/[bank]/page.tsx:9` | 13 |
| B8 | `BANK_INFO` (+founded/hq/cards_issued) | `app/bank/[slug]/page.tsx:19` | 13 |
| B9 | `NETWORK_BY_BANK` | `CardsClient.tsx:7`, `bank/[slug]:10`, `best-cards/[category]:39`, `smart-match:17` | 4 copies, 3 value-sets |
| — | `CARD_COUNT=49`, `BANK_COUNT=12` | `lib/catalogue-stats.ts:10-11` | derived constants (test-guarded) |

### 1.5 CONTRADICTION MATRIX

Cards:
| Pair | Conflict |
|------|----------|
| SEED_CARDS (49) vs **NEW_CARDS** (dead, 76) | Dead block holds better-sourced rewrites of live cards; 12 colliding ids (see Section 2). Editing NEW_CARDS = zero runtime effect (landmine). |
| SEED_CARDS vs **CARDS_WITH_POINTS** | Parallel id-space: `axis-magnus`≠`axis-magnus-burgundy`, `idfc-wealth`≠`idfc-first-wealth`, `standard-chartered-ultimate`≠`sc-ultimate`, `hdfc-tata-neu-infinity`≠`tata-neu-infinity-hdfc`; +5 ids not in the live 49. Offers cards the rest of the app can't resolve. |
| SEED_CARDS vs **CARD_POINT_VALUES** | 3rd id-space: `hdfc-infinia-metal`/`hdfc-regalia`/`hdfc-diners-privilege` not in SEED; RAG compensates by name-matching. |
| SEED_CARDS vs **APR_MAP** (51 keys) | ~8 phantom keys match no live card → silent no-op patch. |
| SEED_CARDS vs **LADDER** | `idfc-first-classic` dead link; drifted names/fees (mislabels `amex-platinum-travel` as "Amex Platinum Charge Rs.60,000"). |
| **EDITORIAL_IDS** (6) vs **CURATED** (6) | Two different "featured 6" sets (EDITORIAL has regalia-gold; CURATED has sbi-cashback). |
| SEED_CARDS vs scraper slugs vs cards-sync prompt | 5th/6th id-spaces; extras (`shoppers-stop`, `irctc-credit-card`, `OneCard`) in no other registry. |

Per-point value (same programme, ₹/point):
| Programme | seed-cards | redemption.ts (R4) | point-values (C7) | loyalty-tracker (R6) | Conflict |
|-----------|-----------|--------------------|-------------------|----------------------|----------|
| KrisFlyer (HDFC 1:1) | **1.8** | **1.60** | unknown (refuses; >₹1 issuer max) | **1.6** | seed over-claims past both others |
| Marriott Bonvoy | **1.3** | **1.2** | unknown | **0.7** | ~2× spread |
| Avios (Amex) | — | 1.2 | — | **1.5** | disagree |
| Air India FR (SBI) | **0.5** | **0.60** | — | 0.8 | disagree |

Transfer ratios (Axis EDGE → KrisFlyer) — **3 live values**: `TRANSFER_EDGES` (R2) = **5:2** · `redemption.ts` (R4) = **5:4** · `trip-compare TRANSFER_PARTNERS` (R7) = **1.0**.

Airline slug vocabularies (same airline, 3 vocabularies): `Air India` = canonical `air-india` (R2/R3/A16) vs `PROGRAM_TO_SOURCE` **`flyingblue`** (admitted wrong) vs travel-ai `air-india`. `PROGRAM_TO_SOURCE` is **self-inverted** (`KrisFlyer→singapore` yet `Singapore Airlines→krisflyer` adjacent). `AIRLINE_NAMES` code **`UK`** = "Vistara" in `fares.ts` but "Air India" in `trip-compare`.

City→IATA:
- **GOA/GOI live bug on a money surface:** city maps do `goa→GOI`, but `FLIGHT_PRICE_RANGES`/`HOTEL_PRICE_RANGES` key Goa as `'GOA'`. `resolveIata()` converts to GOI before lookup → **Goa always falls through to `default` pricing** in trip-cost estimates. (The file comments the hazard 30 lines above.)
- 4 primary city→IATA maps disagree on coverage while the 3,242-row canonical `resolveCity()` sits unused.

Airports: **A8 vs A10** = two different "top 8 Indian airports" (A8 has PNQ/Pune, A10 has COK/Kochi). A1 (3,242) is authoritative but only 2 components use it.

Banks: 5 issuer lists with membership **6 vs 12 vs 13** and inconsistent hex colors; 2 separate bank-metadata registries (BANK_META vs BANK_INFO); `NETWORK_BY_BANK` in 4 files with 3 value-sets.

**Highest-risk items:** (1) GOA/GOI pricing fall-through on a money surface; (2) three conflicting Axis→KrisFlyer ratios; (3) NEW_CARDS holding better data than production while shipping to nobody; (4) `point-values.ts` (the honesty "moat" table) being the only value store that respects issuer ceilings while 4 others quote higher sweet-spot values.

---

## SECTION 2 — Complete NEW_CARDS inventory

> Source: full hand read of `lib/data/seed-cards.ts:853–2633` (every entry, no indent filter).

### Structure & counts
`NEW_CARDS` (declared `:853`, closes `:2633`, **never spread into SEED_CARDS**) has two internally-formatted parts:
- **Compact one-liner block** `:855–:932` — **44 entries**
- **Verbose block** (`BATCH 1`/`BATCH 2`) `:937–:2630` — **38 entries**

**Total physical entries = 82. Distinct ids = 76** (6 ids appear twice inside NEW_CARDS itself). This matches the reported 76. The earlier "38 of 76" partial capture only saw the verbose block and missed all 44 compact one-liners.

### 2.1 CRITICAL — the two known catalogue gaps

**Neither plain "HDFC Regalia" nor plain "Axis Magnus" exists in NEW_CARDS — or anywhere else in the repo.** The hypothesis that NEW_CARDS "may be their only source" is **FALSE**. Full-file search (`grep -niE "regalia|magnus"` on `seed-cards.ts`) returns only:

- `hdfc-regalia-gold` / name **"HDFC Regalia Gold"** — `lib/data/seed-cards.ts:64–66` — this is a **live SEED_CARDS** entry, the *Gold* variant, not plain Regalia.
- `axis-magnus-burgundy` / name **"Axis Magnus for Burgundy"** — `lib/data/seed-cards.ts:250–252` — a **live SEED_CARDS** entry, the *Burgundy* variant, not plain Magnus.
- The only other hits are prose mentions ("alternative to HDFC Regalia Gold" `:2261`; "without Magnus income requirement" `:768`) — not card definitions.

So both gaps remain genuinely unfilled. There is **no full definition to paste** for either plain card — they do not exist in any registry. Sourcing them is net-new editorial work, not a promote-from-NEW_CARDS task.

### 2.2 Full distinct-id inventory (76)

Legend for "Also in": **LIVE** = collides with a SEED_CARDS/MORE_CARDS id (the live 49) · **NEW×2** = appears twice inside NEW_CARDS · **—** = nowhere else (unique to this dead block) · **near-dup** = same real card as a live entry under a different slug.

Compact block (`:855–:932`):
| id | line | Also in |
|----|------|---------|
| hdfc-diners-clubmiles | 855 | — |
| hdfc-platinum-times | 856 | — |
| hdfc-indianoil | 857 | — |
| sbi-simplysave | 860 | — |
| sbi-pulse | 861 | — |
| sbi-prime | 862 | — |
| sbi-club-vistara | 863 | — |
| sbi-aurum | 864 | NEW×2 (also :1495) |
| icici-mmt-platinum | 867 | — |
| icici-mmt-signature | 868 | — |
| icici-hpcl-super-saver | 869 | — |
| icici-expresspay | 870 | — |
| axis-reserve | 873 | NEW×2 (also :1352) |
| axis-airtel | 874 | — |
| axis-indianoil | 875 | — |
| axis-neo | 876 | — |
| kotak-white | 879 | — |
| kotak-privy-league | 880 | — |
| kotak-essentia-platinum | 881 | — |
| amex-smartearn | 884 | — |
| idfc-club-vistara | 887 | — |
| idfc-first-wow | 888 | — |
| idfc-first-millennia | 889 | — |
| rbl-dubai-first | 892 | — |
| rbl-icon | 893 | — |
| rbl-fun-plus | 894 | — |
| yes-prosperity-cashback | 897 | — |
| yes-ace | 898 | — |
| indusind-nexxt | 901 | — |
| indusind-legend | 902 | — |
| sc-manhattan | 905 | — |
| sc-super-value-titanium | 906 | — |
| au-altura | 909 | — (live has `au-altura-plus`, different) |
| au-vetta | 910 | — |
| federal-signet | 913 | — |
| federal-celesta | 914 | — |
| hsbc-cashback | 917 | — |
| hsbc-premier | 918 | NEW×2 (also :2543) |
| hsbc-live-plus | 919 | NEW×2 (also :1266) |
| onecard | 922 | NEW×2 (also :2276) |
| bajaj-rbl-supercard | 925 | — |
| idbi-steel | 928 | — |
| bob-eterna | 931 | NEW×2 (also :1760) |
| bob-prime | 932 | — |

Verbose block (`:937–:2630`):
| id | line | Also in |
|----|------|---------|
| icici-emeralde-private-metal | 938 | — (live has `icici-emeralde`, different) |
| icici-sapphiro | 990 | **LIVE** |
| icici-rubyx | 1039 | **LIVE** |
| indusind-pinnacle | 1082 | **LIVE** |
| indusind-tiger | 1135 | — |
| indusind-avios | 1170 | — |
| hsbc-travelone | 1215 | — |
| hsbc-live-plus | 1266 | NEW×2 (also :919) |
| axis-ace | 1309 | **LIVE** |
| axis-reserve | 1352 | NEW×2 (also :873) |
| kotak-white-reserve | 1405 | — |
| kotak-indigo-6e | 1458 | — |
| sbi-aurum | 1495 | NEW×2 (also :864) |
| sbi-simply-click | 1550 | near-dup (live `sbi-simplyclick`, diff spelling) |
| amex-mrcc | 1596 | **LIVE** |
| amex-gold-charge | 1645 | — (live has `amex-gold`, different) |
| sc-easemytrip | 1687 | — |
| sc-rewards-plus | 1721 | — |
| bob-eterna | 1760 | NEW×2 (also :931) |
| bob-etihad-guest-premium | 1805 | — |
| scapia-federal | 1851 | — |
| kiwi-axis-rupay | 1893 | — |
| hdfc-swiggy-blck | 1931 | — (live has `hdfc-swiggy`, diff product) |
| hdfc-diners-privilege | 1971 | — |
| idfc-first-power-plus | 2014 | — |
| rbl-world-safari | 2058 | — |
| rbl-shoprite | 2105 | **LIVE** |
| yes-marquee | 2138 | **LIVE** |
| au-altura-plus | 2186 | **LIVE** |
| au-zenith-plus | 2226 | — (live has `au-zenith`, different) |
| onecard | 2276 | NEW×2 (also :922) |
| axis-vistara-infinite | 2317 | **LIVE** |
| hdfc-moneyback-plus | 2371 | **LIVE** (dead 0.67% vs live shipped ~2%) |
| idfc-first-classic | 2414 | **LIVE** |
| amazon-pay-icici | 2459 | near-dup (live `icici-amazon-pay`, same card) |
| axis-flipkart | 2501 | **LIVE** |
| hsbc-premier | 2543 | NEW×2 (also :918) |
| indusind-eazydiner-platinum | 2592 | — |

### 2.3 Cross-reference roll-up
- **12 ids collide with the live 49** (would need de-dup before any `SEED_CARDS.push(...NEW_CARDS)`): `icici-sapphiro`, `icici-rubyx`, `indusind-pinnacle`, `axis-ace`, `amex-mrcc`, `rbl-shoprite`, `yes-marquee`, `au-altura-plus`, `axis-vistara-infinite`, `hdfc-moneyback-plus`, `idfc-first-classic`, `axis-flipkart`.
- **6 ids are duplicated inside NEW_CARDS itself:** `sbi-aurum`, `axis-reserve`, `hsbc-premier`, `hsbc-live-plus`, `onecard`, `bob-eterna` (compact one-liner + a later richer verbose entry).
- **2 near-dups** (same real card, different slug — not exact id collisions but would double-list a product): `sbi-simply-click`↔live `sbi-simplyclick`; `amazon-pay-icici`↔live `icici-amazon-pay`.
- **~56 ids exist nowhere else** — genuinely new catalogue candidates trapped in dead code (Scapia, Kiwi UPI, HSBC TravelOne, BOB Eterna LTF, Amazon Pay ICICI, HDFC Swiggy BLCK, IDFC Power+, etc.).
- Confirmed richer-than-live rewrite noted in code: `hdfc-moneyback-plus` here carries the correct **0.67%** base (`:2383`) vs the live catalogue's historically-wrong rate; `indusind-pinnacle` here carries the corrected 0.75% base (`:1099`).

---

## SECTION 3 — Every silent-fallback site (lib/ + app/api/)

> Source: search agent. Ranked worst-first by how wrong the displayed value could be. Money/points surfaces prioritized.

| # | Site | Trigger | What the user sees | Can user tell? | How wrong |
|---|------|---------|--------------------|----------------|-----------|
| 1 | `app/api/trip-planner/route.ts` (whole handler; airline coercion **:249**) | Every request — entire flight/hotel result (`pointsNeeded`, `cashPrice`, `pointsValue`, `saving`, `available:true`) is **LLM-generated, zero live grounding**. `:249` also silently rewrites any unrecognized domestic airline to `'IndiGo'`. | Precise figures + named airline + "available: true". No estimate label. | **No** | Unbounded — fabricated redemption verdict. Worst money-surface offender. |
| 2 | `app/api/trip-compare/route.ts` (**:9-37, :196-203, :348-395**) | LLM asked to "generate realistic comparison data" within hardcoded price ranges; points math uses hardcoded `0.35`/pt (flight), `0.6`/pt (hotel). Only card #1 grounded; cards 2-3 fabricated. | Rupee + points + "pointsSaving". One-line `priceNote` disclaimer; per-row numbers look exact. | **Weakly** | Large — fabricated fares/points/savings on cards 2-3; made-up valuation constants. |
| 3 | `app/api/subscription/cancel/route.ts:76-79` | Razorpay cancel IS error-checked (502 on fail), but local `cancel_at_period_end` DB update is **not**. | `{ok:true}` → UI "cancelled". | **No** | High but mitigated — period-end webhook reconciles; immediate UI state can be wrong. |
| 4 | `app/api/optimize/route.ts:29` | `points \|\| 50000` — real balance 0/missing. | LLM strategy computed for a **fabricated 50,000-pt balance** as if the user's. | **No** | High — advice sized to a balance the user doesn't have. |
| 5 | `app/api/rewards-calculator/route.ts:14` | Card missing category rule → `card.base_reward_rate \|\| 1`. Reads unreliable Supabase `cards` table; `percent` vs `multiplier` branches do identical math (multiplier unhandled). | Monthly-rewards figure + "switch to this card". | **No** | Moderate–high — silent 1% default + mishandled multipliers can flip the switch recommendation. |
| 6 | `app/api/travel-ai/route.ts:176` | `origin \|\| 'BOM'` when origin not extractable. | seats.aero availability for **BOM→dest** fed to LLM as "LIVE AWARD AVAILABILITY", even if user never said Mumbai. | **No** | Moderate — real data, wrong origin, presented as live fact. |
| 7 | `app/api/card-switch/route.ts:104-118` | Every request — `monthlyValue`/`annualUpgrade`/`annualFee` are LLM numbers (no `engine.ts` grounding). | "Switch and gain ₹X/year". | **No** | Moderate–high — `calculateAnnualValue` exists but is bypassed. |
| 8 | `lib/redemption.ts:237-239` | `getFloorValuation` finds no deterministic channel → returns `rate 0.20, via:'statement credit'`. | "worth at least ₹X" floor. | **No** (comment: "should not happen") | Low–moderate — fabricated 0.20/pt floor if tables ever incomplete. |
| 9 | `app/api/aa/callback/route.ts:25` + `aa/consent` + `aa/fetch-data` | AA consent/status/linked-card writes not error-checked; redirect to `?linked=success`. | "Linked · success" + balances. | **No** | Would be CRITICAL (verified-green points that never persisted) **but gated off by `aaEnabled()` kill-switch** — latent, not live. |
| 10 | `alerts/subscribe` (~:22-27), `ig-click:15`, `track-click:14`, `user-city` POST, `ratings:20` | Supabase write not error-checked / `catch{return ok:true}`. | "Subscribed"/"Saved"/rating recorded. | **No** | Low money impact (non-financial), but success is asserted when it may have failed. Matches known swallowed-write backlog. |
| 11 | `employee/join-org` (~:58), `employee/workplace` (~:50,57) | `personal_entitlements` upsert/delete not error-checked; `ok:true`. | "Corporate sponsored" granted / "unlinked". | **No** | Moderate — a paid-benefit entitlement can silently diverge from the UI claim. |

### What is clean (do NOT "fix" these — they are honest by design)
- `lib/fusion-core.ts`, `lib/transfer-ladder.ts`, `lib/transfer-map.ts` — resolver-miss returns `currency-unknown`/`not-transferable`, never a guessed ratio; all ship `verified:false`.
- `lib/data/point-values.ts` — build-gated, integer-paise, returns `null` for unknown, never over-claims past issuer ceiling.
- `lib/engine.ts` — `?? 7` rating / `|| 1.0` redemption defaults are documented internal scoring assumptions, not displayed as the user's money.
- `lib/cheapest-cash.ts` + `flights/cheapest`, `hotels/search`, `trip-planner/live-price`, `flights/cash-price`, `fares`, `feed`, `ticker`, `intelligence` — degrade to null/empty with an honest source label.
- `parse-statement`, `update-points`, `manual-cards`, `onboarding`, `first-run`, `profile`, `razorpay/verify*`, `wa/redeem`, `report-card` — DB write errors ARE surfaced (non-2xx). `parse-statement` explicitly refuses to fake "Saved".

**Top exposure:** `trip-planner` and `trip-compare` — fully/partly LLM-invented rupee-and-points redemption verdicts on the core travel money surface, with only a soft disclaimer. (Ties to memory items [[dont-fake-saved-check-db-write-errors]] and the honesty moat.)

---

## SECTION 4 — Transfer graph coverage

> Source: full hand read of `lib/data/transfer-graph.ts` + search agent for seats.aero slugs. **No edges added** — sourcing ratios is human editorial work per the provenance rule.

### 4.1 Every edge in the shipped graph (5 edges, 3 destination programmes)

All 5 edges are `state:'unverified'` (render neutral grey, never green), all `duration_days = null`, all `min_transfer = null`.

| # | from_currency | to_programme | ratio | source | as_of | allowlist | line |
|---|---------------|--------------|-------|--------|-------|-----------|------|
| 1 | `axis_edge` | `singapore` | 5:2 | `axis-edge-rewards-devaluation-2026-04-02` | 2026-04-02 | null | :45 |
| 2 | `axis_miles` | `singapore` | 1:1 | `internal-estimate:lib/transfer-map.ts` | 2026-07-10 | null | :61 |
| 3 | `hdfc_reward_points` | `singapore` | 1:1 | `internal-estimate:lib/transfer-map.ts` | 2026-07-10 | `['HDFC Infinia Metal Edition','HDFC Diners Club Black']` | :79 |
| 4 | `axis_edge` | `air-india` | 1:1 | `internal-estimate:lib/transfer-map.ts` | 2026-07-10 | null | :96 |
| 5 | `amex_membership_rewards` | `ba` | 1:1 | `internal-estimate:lib/transfer-map.ts` | 2026-07-10 | null | :112 |

Destination programmes reachable: **`singapore`, `air-india`, `ba`** (3). Edge #1's ratio was re-rated from 5:4→5:2 for the 2 Apr 2026 Axis EDGE devaluation. Edges #4/#5 carry code comments flagging the slugs (`air-india`, `ba`) as best-guess/unconfirmed against live seats.aero.

### 4.2 Programme slugs seats.aero can return

Representation: the slug is carried on `SeatsAeroResult.source` (`lib/seats-aero.ts:26`, from raw `item.Source` at `:138`); the transfer lookup keys `award.source` as `to_programme` (`lib/transfer-ladder.ts:27,210,220-224`). The authoritative "sources our cached search returns" list is the display map `SOURCE_PROGRAM_LABEL` (`lib/fusion-core.ts:22-48`) — **25 slugs**:

`singapore`, `air-india`, `ba`, `united`, `aeroplan`, `alaska`, `velocity`, `aadvantage`, `delta`, `emirates`, `etihad`, `flyingblue`, `virginatlantic`, `jetblue`, `lifemiles`, `qantas`, `turkish`, `qatar`, `saudia`, `smiles`, `connectmiles`, `azul`, `ethiopian`, `eurobonus`, `finnair`.

There are **no seats.aero fixtures / mock JSON** in the repo (the only test file `lib/transfer-ladder.test.ts` uses inline synthetic edges: destinations `singapore`/`ba`, synthetic hotel nodes `accor`/`marriott`). Two other request-side slug maps use *different* vocabularies: `PROGRAM_TO_SOURCE` (`lib/seats-aero.ts:8`, adds `krisflyer`/`american`) and `AIRLINE_TO_PROGRAM` (`app/api/travel-ai/route.ts:29`, adds `avios`/`mileageplus`/`skymiles`/`asia-miles`/`miles-and-more`/`indigo`).

### 4.3 GAP LIST — programmes seen but with NO transfer edge

Of the 25 returnable programmes, only 3 have edges. **22 have zero transfer edge (88% gap):**

`united`, `aeroplan`, `alaska`, `velocity`, `aadvantage`, `delta`, `emirates`, `etihad`, `flyingblue`, `virginatlantic`, `jetblue`, `lifemiles`, `qantas`, `turkish`, `qatar`, `saudia`, `smiles`, `connectmiles`, `azul`, `ethiopian`, `eurobonus`, `finnair`.

A card holder searching these will see the award seat but hit `not-transferable` / "no verified transfer route" (`fusion-core.ts:210-216`, `Board.tsx:550-551`).

Additional latent risk — **inconsistent alias slugs**: even a conceptually-supported partner can miss its edge if a response is tagged with an alias the graph doesn't match — e.g. `krisflyer` (canonical is `singapore`), `avios` (canonical `ba`), `american` (canonical `aadvantage`), plus `asia-miles`/`miles-and-more`/`mileageplus`/`skymiles`/`indigo` from the travel-ai map. And `PROGRAM_TO_SOURCE` maps "Air India Flying Returns"→`flyingblue` (`seats-aero.ts:11`), which `transfer-map.ts:20` explicitly calls wrong (should be `air-india`).

**No edges were added. Sourcing real ratios + durations is a human task per the provenance rule in `transfer-graph.ts:8-25`.**

---

*End of audit. Read-only; only this file created. No commits, pushes, schema changes, or deletions performed.*
